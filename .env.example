# Public inbound webhook the reserve form posts to (Make.com scenario).
# Public by nature — it ships in the browser bundle — so the Make scenario
# must validate what it receives. Rotate here rather than in source.
PUBLIC_LEAD_WEBHOOK=https://hook.eu1.make.com/your-webhook-id
